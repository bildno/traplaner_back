# application.yml보다 우선순위가 높은 설정 파일과 읽을 설정 파일 등록
spring:
  application:
    name: travelplan-service
  cloud:
    config:
#      uri: http://localhost:8888
      uri: http://config-service:8888
      profile: default